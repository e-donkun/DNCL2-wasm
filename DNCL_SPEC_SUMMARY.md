# DNCL 文法リファレンス（実装者向け要約）

原典: 大学入試センター「共通テスト手順記述標準言語（DNCL）の説明」（2022年1月、以下「旧仕様」）
および 大学入試センター「6-1 概要『情報』」18〜24ページ「共通テスト用プログラム表記の例示」
（2022年11月改訂、以下「新仕様」）
https://www.dnc.ac.jp/albums/abm.php?d=744&f=abm00003141.pdf

このファイルは原文の逐語コピーではなく、インタプリタ実装のために文法規則を
独自に整理し直したものです。正確な原文表現が必要な場合は上記PDFを参照してください。

**このプロジェクトは新仕様を主として実装しており、新旧で競合しない範囲では旧仕様も
後方互換として受け付けます。** 新旧で表記が競合する部分（代入演算子、配列リテラルの
括弧、比較演算子の等号、論理演算子など）は新仕様を優先し、旧表記は原則として使えません
（例外: 配列リテラル `{}` と代入の `←` は競合しないため両方使えます）。

## 1. 変数・値

- 変数名: 英字始まり＋英数字＋`_` の並び（ASCII識別子）。
  - 小文字始まり = スカラー変数の慣例
  - 大文字始まり = 配列の慣例（厳密な文法制約ではない）
- 配列の添字は `[` `]` で指定。多次元は `,` 区切り。例: `Tokuten[3]`、`Data[2,4]`
  - **添字は0から始まる**（新仕様で明記）
- 数値: 10進数、整数・小数（`123`, `3.14`）
- 文字列: `"..."` で囲む（旧仕様の `「...」` も後方互換で使用可）

## 2. 文字列

- ダブルクォーテーションで囲む: `moji = "I'll be back."`
- `+` で文字列同士を連結できる: `message = "祇園精舎の" + "鐘の声"`

## 3. 代入文

- 基本形: `変数 = 式`（**新仕様。旧仕様の `←` も後方互換で使用可**）
- 配列要素: `配列名[添字, ...] = 式`
- 配列の一括初期化（配列リテラル）: `配列名 = [式, 式, ...]`
  （旧仕様の `{式, 式, ...}` も後方互換で使用可）
- 配列全要素への代入: `配列名 のすべての値を 式 にする`
  （旧仕様の `配列名 のすべての要素に 式 を代入する` も後方互換で使用可）
- 複数の代入文は `,` で区切って同じ行に並べられる（左から順に実行）:
  `kosu = 3 , kingaku = 300`
- 外部入力: `変数 =【外部からの入力】`
- 増減の糖衣構文（`変数を式増やす/減らす`）は**新仕様では廃止**。
  `変数 = 変数 + 式` のように直接書く。旧仕様の糖衣構文は後方互換のため使用可。

## 4. 演算子

### 4.1 算術演算子（優先順位: 高→低）
1. べき乗 `**`（新仕様。右結合。単項マイナスより強く結合する: `2 ** -1` は可）
2. 単項マイナス
3. `*`（乗算） `/`（実数除算） `÷`（整数商） `%`または`％`（剰余）
4. `+` `-`

括弧 `(` `)` で優先順位を明示できる。左結合。
（旧仕様の全角記号 ×／－＋ も後方互換で使用可）

### 4.2 比較演算子（数値・文字列共通）
`==`（等しい） `!=`（等しくない） `>` `>=` `<` `<=`

**新仕様では `==` が等価比較、`=` は代入**である点に注意（旧仕様は逆に近い運用だった）。

### 4.3 論理演算子
`and`（論理積） `or`（論理和） `not`（否定、前置）

**評価順序（実装判断）**: 原文（新仕様PDF）には評価順序の明記がない。
Python3との対比表が用意されるなど新表記はPythonに強く準拠しているため、
本実装では一般的なプログラミング言語と同じ優先順位・結合規則を採用した:

```
not > and > or         （not は前置の単項演算子、and/orは左結合）
```

例: `a or b and c` は `a or (b and c)` として評価される（`(a or b) and c` ではない）。

※旧仕様の `かつ` `または` `でない` は**新仕様では廃止**（`and`/`or`/`not` に統一）。
旧仕様における「優先順位なし・出現順に逐次評価」というルールも、この一本化に伴い
本実装では採用していない。

## 5. 制御文

### 5.1 条件分岐文（if / elif / else）

新仕様（コロン + インデント。Pythonに類似）:
```
もし 〈条件〉 ならば:
    〈処理〉
そうでなくもし 〈条件2〉 ならば:
    〈処理2〉
そうでなければ:
    〈処理3〉
```
- ブロックはインデント（半角スペース推奨）で表す。
- `そうでなくもし` は任意個チェーン可能（else-if の連鎖）。
- 参考PDF上の `｜`・`⎿` はブロック範囲を示す視覚的なガイド線（｜=継続、⎿=末尾）であり、
  本実装ではこれらの文字もインデントの一種として受け付ける
  （PDFからコピーした行がそのまま動くようにするため）。

旧仕様（閉じ語方式。後方互換で使用可）:
```
もし 〈条件〉 ならば
    〈処理1〉
を実行し，そうでなくもし 〈条件2〉 ならば
    〈処理2〉
を実行し，そうでなければ
    〈処理3〉
を実行する
```
`ならば` の直後が `:` かどうかで新旧どちらの形式かを判別する。

### 5.2 条件繰返し文（while、前判定のみ）

新仕様:
```
〈条件〉 の間繰り返す:
    〈処理〉
```

旧仕様（後方互換）:
```
〈条件〉 の間，
    〈処理〉
を繰り返す
```

※旧仕様にあった後判定ループ（`繰り返し，〈処理〉を，〈条件〉になるまで実行する`）は
新仕様に記載がないが、後方互換として引き続きサポートする。

### 5.3 順次繰返し文（for）

新仕様:
```
変数 を 初期値 から 終了値 まで 差分 ずつ増やしながら繰り返す:
    〈処理〉
```

旧仕様（後方互換）:
```
変数 を 初期値 から 終了値 まで 差分 ずつ増やしながら，
    〈処理〉
を繰り返す
```

手順は新旧共通: (1) 変数←初期値 (2) 変数>終了値なら終了 (3) 処理を実行し変数+=差分、(2)へ。
`ずつ増やしながら` の代わりに `ずつ減らしながら` を使うと、終了条件が反転する
（変数が終了値を下回るまで減算しながら繰り返す）。

## 6. 関数呼び出し

呼び出し構文: `関数名(引数1, 引数2, ...)`（関数名は英数字識別子でも日本語の語でもよい）

### 6.1 表示

新仕様では表示は関数呼び出し形式に統一された:
```
表示する(式1, 式2, ...)
```
カンマ区切りの各式を**区切り文字なしでそのまま連結**して1行出力する。
（旧仕様の `式 と 式 を表示する` という後置構文は新仕様では廃止）

### 6.2 組み込み関数

新仕様PDFに明記されている例:
- `表示する(...)`: 値を返さない手続き。上記の通り
- `要素数(配列)`: 配列の要素数を返す
- `整数(x)`: 0方向への切り捨て（Pythonの`int()`相当）。例: `整数(乱数()*6)+1` でサイコロの目
- `乱数()`: **引数なし**。0以上1未満の実数乱数を返す（旧仕様と挙動が異なる点に注意）

「表示する」以外の関数は基本的に問題文中で個別に説明される、との注記がある
（＝汎用的な組み込み関数として仕様上固定されているわけではない）。

### 6.3 後方互換の組み込み関数（旧仕様、新仕様には記載なし）

- `二乗(x)`
- `べき乗(m, n)`（新仕様では `**` 演算子に置き換えられたが後方互換で存続）
- `乱数(m, n)`: m以上n以下の整数乱数を返す（引数2個で呼んだ場合のみ。0引数なら新仕様の挙動）
- `奇数(n)`: nが奇数なら真
- `二進で表示する(n)`: nを2進表現で表示する

### 6.5 追加の組み込み関数（Pythonの組み込み関数に倣って本実装で追加）

- `最大値(x, y)` / `最大値(配列)`: Pythonの`max()`に倣い、2引数（`最大値(1, 5)`）と
  配列1個（`最大値(Data)`、配列要素の中の最大値）のどちらの呼び出し方にも対応する
- `最小値(x, y)` / `最小値(配列)`: 同様に`min()`相当。呼び出し方は`最大値`と同じ
- `切り上げ(x)`: 小数点以下を切り上げる（Pythonの`math.ceil()`相当）
- `切り捨て(x)`: 小数点以下を切り捨てる（Pythonの`math.floor()`相当。負の無限大方向への
  切り捨てであり、0方向への切り捨てを行う`整数(x)`とは挙動が異なる点に注意）
- `四捨五入(x)`: 小数点以下を四捨五入する

## 7. 関数定義（旧仕様、後方互換）

新仕様PDFにはユーザー定義関数の構文が記載されていない
（＝各問題文で個別に提供される想定）。旧仕様の定義構文は引き続きサポートする:

```
関数 関数名 ( 引数列 ) を
    〈処理〉
と定義する
```
- 引数はカンマ区切り
- 定義した関数は組み込み関数と同じ記法で呼び出せる
- 戻り値は、関数名と同じ名前のローカル変数に代入された値とする（本実装独自の判断。
  原文に明示のreturn構文がないため）

## 8. コメント

`#` から行末までをコメントとして無視する（新仕様で明記）。

## 9. 実装上の判断ポイント（原文に明記がなく本実装で決定した事項）

- **論理演算子の優先順位**: 4.3節参照（not > and > or を採用）
- **ブロックの実際の入力方法**: インデント（半角スペース）方式を正式にサポートし、
  PDF上の `｜`・`⎿` に加え `|`・`│`・`┃`・`└`・`┗` もインデント相当として許容する
- **式の途中での改行**: `()`・`[]`・`{}` が閉じていない間の改行は行の継続として扱う
  （Pythonの暗黙の行継続と同様）。問題文の作例で式の途中に改行が入っていても
  1つの論理行として解釈できるようにするため
- **配列添字の範囲外アクセス・型不一致**: 実行時エラーとして打ち切る
- **関数の戻り値の受け渡し方法**: 6.4節/7節参照
- **真偽値の表示**: `真`/`偽` と表示する（新仕様の作例では0/1の数値フラグで代用する
  書き方も見られるが、言語機能としての真偽値表示はこの規則とする）
- **全角/半角記号の扱い**: 旧仕様由来の全角記号（＋－×／等）は後方互換のため
  新仕様のASCII記号と併用可能とした
- **原文に定義のない挙動はPython3に準拠する**（本実装の方針）:
  - 真偽判定（もし文の条件など）は数値・真偽値だけでなく文字列・配列も対象とし、
    空文字列/空配列は偽、それ以外は真とする（Pythonの`bool()`と同じ）
  - `and`/`or` は短絡評価する（右辺に副作用があっても左辺で結果が決まれば評価しない）
  - `/`（実数除算）のゼロ除算はエラーとする（Pythonの`ZeroDivisionError`相当）
  - 文字列にも添字アクセス（`s[0]`）や`要素数(s)`（文字数）を使える
    （Pythonの文字列インデックス・`len()`相当）
  - `整数(x)` は文字列にも適用でき、数値に変換できなければエラーとする
    （Pythonの`int(str)`相当）

## 10. 外部からの入力のUX（実装上の工夫）

`【 】`自体がPythonの`input()`にあたり、`【 】`内に書かれた文字列
（例: `【外部からの入力】`の「外部からの入力」）はPythonの`input(prompt)`の
プロンプト引数と同様に扱われる。`dist/index.html` のコンソールでは、
`【 】`に到達した時点でまずそのプロンプト文字列を出力欄に表示し、
続けてその場に入力欄と`[↵]`ボタンを表示する。Enterキーを押すか
`[↵]`ボタンをクリックすると入力が確定し、続きが実行される。
WASM側の実行は完全に同期的で途中停止できないため、「まだ答えていない
入力が必要になったら実行を中断し、これまでに得た回答をすべて使い回し
ながら最初から実行し直す」というリプレイ方式で実現している。
`乱数()`は1回の実行セッション中は同じ乱数シードを使い回すことで、
再実行のたびに結果が変わらないようにしている。

## 11. 配列添字の開始番号（0始まり/1始まり）の切替

原文（新仕様PDF）の配列添字は0始まりが基本だが、問題によっては1始まりを
前提とした作例も見られるため、`dist/index.html`の「ソースコード」見出し横の
プルダウンで実行時に切り替えられるようにした。「配列の添字は1から始まる」を
選ぶと、`Data[1]`が配列の先頭要素（内部的には0番目の要素）を指すようになる。

- 影響が及ぶのは配列・文字列への添字アクセス（`Data[i]`、代入先としての
  `Data[i] = ...`、文字列の`s[i]`）のみ。`要素数()`が返す値（配列の長さ）や
  `for`文の範囲指定（`i を 2 から 6 まで`のようにユーザーが書いた数値そのもの）
  には影響しない
- WASM側は`assembly/interpreter.ts`の`Interpreter.indexBase`（0または1）として
  保持し、`assembly/index.ts`の`runProgram(src, indexBase)`の第2引数として
  ホスト側から明示的に渡す
- 共有URL/QRコードにもこの設定を含める。ハッシュの先頭1文字を添字開始番号
  ("0"/"1")とし、続けてソースコードを格納することで、URLを開いたときに
  ソースコードと添字設定の両方を復元できるようにしている
