# DNCL 文法リファレンス（実装者向け要約）

原典: 大学入試センター「共通テスト手順記述標準言語（DNCL）の説明」（2022年1月）
https://www.dnc.ac.jp/albums/abm.php?d=666&f=abm00000819.pdf

このファイルは原文の逐語コピーではなく、インタプリタ実装のために文法規則を
独自に整理し直したものです。正確な原文表現が必要な場合は上記PDFを参照してください。

## 1. 変数・値

- 変数名: 英字始まり＋英数字＋`_` の並び（ASCII識別子）。
  - 小文字始まり = スカラー変数
  - 大文字始まり = 配列（慣例。厳密な文法制約ではない）
  - 全て大文字 = 実行中不変な値（定数扱い、慣例）
- 配列の添字は `[` `]` で指定。多次元は `,`（全角推奨、半角も許容すべき）区切り。
  例のイメージ: `A[2]`、`B[3, 2]`
- 数値: 10進数、整数・小数（`123`, `3.14`）
- 文字列: `"..."` または `「...」` で囲む

## 2. 表示文

`式 (と 式)* を表示する`

- 複数の式は「と」で連結し、最後に「を表示する」で終える
- 表示時は各値を**そのまま連結**する（区切り文字は自動挿入されない）

## 3. 代入文

- 基本形: `変数 ← 式`
- 配列要素: `配列名[添字, ...] ← 式`
- 配列の一括初期化: `配列名 ← {式, 式, ...}`
- 配列全要素への代入: `配列名 のすべての要素に 式 を代入する`
- 複数の代入文は `,` で区切って同じ行に並べられる（左から順に実行）
- 増減の糖衣構文:
  - `変数 を 式 増やす` ≡ `変数 ← 変数 + 式`
  - `変数 を 式 減らす` ≡ `変数 ← 変数 - 式`
- 外部入力: `変数 ←【外部からの入力】`

## 4. 演算子

### 4.1 算術演算子（優先順位: 高→低）
1. 単項マイナス
2. `×` `/`（実数除算）`÷`（整数商）`％`（剰余）
3. `+` `-`

括弧 `(` `)` で優先順位を明示できる。左結合。

### 4.2 比較演算子（数値・文字列共通）
`=` `≠` `>` `≧` `≦` `<`
（`≠` は原文中で複数の字形バリエーションが使われることがある）

### 4.3 論理演算子
`かつ` `または` `でない`

**重要な評価規則**: 論理演算子には優先順位がなく、**左から出現順に逐次評価**される。
`でない` は後置の単項演算子で、直前までに構築された式全体に対して否定を適用する。
括弧で明示的にグループ化しない限り、次のように評価される:

```
A かつ B でない   →  (A かつ B) でない     ※ (A かつ (B でない)) ではない
A かつ B または C →  ((A かつ B) または C)
```

実装方針: `acc = term(); while (次が かつ|または|でない) { ... }` という
フラットな左結合ループで実装すればこの規則を自然に満たせる（詳細はDESIGN.md）。

## 5. 制御文

制御文の中身（〈処理〉）には、表示文・代入文・値を返さない関数呼び出し・
条件分岐文・条件繰返し文・順次繰返し文を1つ以上並べられる。

### 5.1 条件分岐文（if / elif / else）

一般形（複数行）:
```
もし 〈条件〉 ならば
    〈処理1〉
を実行し，そうでなくもし 〈条件2〉 ならば
    〈処理2〉
を実行し，そうでなければ
    〈処理3〉
を実行する
```

- 〈処理〉が1文だけの場合は同じ行にまとめて1行で書ける
  （例: `もし 条件 ならば 処理 を実行する`）
- 改行の位置は意味に影響しない（インデントは可読性のためだけで、構文上は
  「を実行する」「を実行し」「と定義する」などの**閉じ語**が構造を決定する）
- `そうでなくもし` は任意個チェーン可能（else-if の連鎖）

### 5.2 条件繰返し文

**前判定（while）**:
```
〈条件〉 の間，
    〈処理〉
を繰り返す
```
条件が真の間、処理の前に判定して繰り返す（0回実行の可能性あり）。

**後判定（do-while）**:
```
繰り返し，
    〈処理〉
を，〈条件〉 になるまで実行する
```
処理の後に判定する（最低1回は実行される）。

### 5.3 順次繰返し文（for）

```
変数 を 初期値 から 終了値 まで 差分 ずつ増やしながら，
    〈処理〉
を繰り返す
```
手順: (1) 変数←初期値 (2) 変数>終了値なら終了 (3) 処理を実行し変数+=差分、(2)へ。

`増やしながら` の代わりに `減らしながら` を使うと、変数が終了値を**下回るまで**
減算しながら繰り返す（終了条件が反転する点に注意）。

## 6. 関数呼び出し

呼び出し構文: `関数名 (引数1, 引数2, ...)`（関数名は英数字識別子でも日本語の語でもよい）

問題文中で用意される代表的な組み込み関数の例（原文で挙げられている例）:
- `二乗(x)`: xの2乗を返す
- `べき乗(m, n)`: mのn乗を返す
- `乱数(m, n)`: m以上n以下の整数乱数を返す
- `奇数(n)`: nが奇数なら真
- `二進で表示する(n)`: nを2進表現で表示する（値を返さない手続き）

これらは問題文ごとに再定義されうる「例」であり、言語仕様として固定ではない点に注意。
実装では合理的なデフォルトとして上記5つを組み込み関数として提供し、
ユーザー定義で上書き可能にするのが実用的。

## 7. 関数定義

```
関数 関数名 ( 引数列 ) を
    〈処理〉
と定義する
```
- 引数はカンマ区切り
- 定義した関数は組み込み関数と同じ記法で呼び出せる
- スコープの厳密な仕様は原文になし → 実装判断が必要（DESIGN.md参照）

## 8. 原文が明言していない実装上の判断ポイント

- ブロック構造の厳密な区切り方法（インデント幅の規定なし。原文の図解は
  「枠」による視覚的表現であり、閉じ語が実際の構文的な終端）
- 関数のスコープ（グローバル変数を関数内で読めるか、代入は局所化されるか）
- コメント構文（原文になし）
- 真偽値リテラル `真`/`偽` を式として書けるか（原文の例では比較演算の結果としてのみ登場）
- 全角/半角記号のどちらまで許容するか（教科書・出題によって混在するため、
  両対応にしておくのが無難）

以上の判断ポイントについては DESIGN.md 内で具体的な実装方針を決め打ちしてある。
